apiVersion: batch/v1
kind: Job
metadata:
  name: sleep
  labels:
    tugbot-kubernetes-events: ReplicaSet.SuccessfulCreate
spec:
  template:
    metadata:
      name: sleep 
    spec:
      containers:
      - name: sleep
        image: selenium
        command: ["sh",  "-c", "python /src/example.py"]
      restartPolicy: Never

